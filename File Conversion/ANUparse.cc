void anuparse(TString directory, TString outName){
  //To run, execute function anuparse("path/to/directory","file_name")
  
  FILE *outFile = fopen(outName,"w");
  
  TSystemDirectory dir(directory,directory);
  TList 
  
  
}