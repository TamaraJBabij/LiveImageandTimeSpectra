#include "DataSet.h"

void checkDensity(DataSet* data, imagingDetectors userDet) {


	// (for every group and) FOR EVERY EVENT (in that group)
			
			int count = 0;

			// FOR EVERY OTHER EVENT (two more fors, then if the events aren't equal)

						// THE FUCK IS THE DISTANCE (just do (x1-x2)^2 + (y1-y2)^2 )
						

						// IF DISTANCE IS LESS THAN SOME NUMBER (start with 10)
							count++;
			// CLOSE THE INNER LOOPS

			// STORE THAT SHIT (count) (you'll need a new int inside group to put it in)




}